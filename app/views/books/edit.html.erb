<%= flash[:notice] %>
<h1>Editing Book</h1>
<%= form_with model: @book, url:update_list_path(@book.id), method: :patch do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Opinion</h4></h4>
  <%= f.text_area :body %>
  
  <P><%= f.submit 'Update Book' %></P>
  
  
<%= link_to 'Show', book_path(@book) %>
|
<%= link_to 'Buck',books_path %>
<% end %>
